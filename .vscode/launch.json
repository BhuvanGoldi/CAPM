{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "cds watch",
      "request": "launch",
      "type": "node-terminal",
      "command": "cds watch",
      "cwd": "${workspaceFolder}",
      "skipFiles": [
        "<node_internals>/**"
      ]
    }
  ]
}